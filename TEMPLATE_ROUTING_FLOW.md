# Template Routing System - Visual Flow

## 🎯 Complete System Flow

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         USER VISITS A URL                               │
│                        (e.g., /, /login, /about)                        │
└────────────────────────────────┬────────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                      REACT ROUTER (App.tsx)                             │
│  • Matches URL to route                                                 │
│  • Uses DynamicRoutes component                                         │
└────────────────────────────────┬────────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                   TENANT STORE (tenantStore.ts)                         │
│  GET /api/v1/tenants/acme-corp/config/                                  │
│                                                                         │
│  Returns:                                                               │
│  {                                                                      │
│    routes: [                                                            │
│      { path: '/', pagePath: '/home', layout: 'none' },                  │
│      { path: '/login', pagePath: '/login', layout: 'none' },            │
│      { path: '/about', pagePath: '/about', layout: 'none' }             │
│    ],                                                                   │
│    theme: { primaryColor: '#ff5722', ... },                             │
│    branding: { ... }                                                    │
│  }                                                                      │
└────────────────────────────────┬────────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────────┐
│              DYNAMIC ROUTES (DynamicRoutes.tsx)                         │
│  • Receives routes array from tenant config                             │
│  • Generates <Route> components dynamically                             │
│  • Maps each route to JsonPageRoute                                     │
│                                                                         │
│  Example:                                                               │
│  <Route path="/" element={<JsonPageRoute pagePath="/home" />} />        │
│  <Route path="/login" element={<JsonPageRoute pagePath="/login" />} />  │
└────────────────────────────────┬────────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────────┐
│              JSON PAGE ROUTE (JsonPageRoute.tsx)                        │
│  • Receives pagePath (e.g., "/login")                                   │
│  • Calls useJsonPages hook                                              │
│  • Renders JsonPage with page config                                    │
└────────────────────────────────┬────────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                USE JSON PAGES (useJsonPages.ts)                         │
│  GET /api/v1/tenants/acme-corp/ui-config/                               │
│                                                                         │
│  Returns:                                                               │
│  {                                                                      │
│    pages: {                                                             │
│      "/home": {                                                         │
│        template: "landing-page",                                        │
│        slots: {                                                         │
│          header: { component: "Navbar", props: {...} },                 │
│          main: { component: "LandingPage", props: {...} },              │
│          footer: { component: "Footer", props: {...} }                  │
│        }                                                                │
│      },                                                                 │
│      "/login": {                                                        │
│        template: "sign-in",                                             │
│        variant: "side-by-side",                                         │
│        slots: {                                                         │
│          main: { component: "SignInForm", props: {...} },               │
│          side: { component: "Card", props: {...} }                      │
│        }                                                                │
│      }                                                                  │
│    }                                                                    │
│  }                                                                      │
└────────────────────────────────┬────────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                  JSON PAGE (JsonPage.tsx)                               │
│  • Receives pageConfig                                                  │
│  • Validates page config                                                │
│  • Passes to PageRenderer                                               │
└────────────────────────────────┬────────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────────┐
│            PAGE RENDERER (@sakhlaqi/ui)                                 │
│  • Reads template type (landing-page, sign-in, marketing-page)          │
│  • Loads corresponding template                                         │
│  • Renders slots with configured components                             │
└────────────────────────────────┬────────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────────┐
│           TEMPLATES (ui/src/core/templates/)                            │
│                                                                         │
│  Landing Page Template:                                                 │
│  ┌───────────────────────────────────┐                                  │
│  │ Header (Navbar)                   │                                  │
│  ├───────────────────────────────────┤                                  │
│  │ Main (Hero + Features + Pricing)  │                                  │
│  ├───────────────────────────────────┤                                  │
│  │ Footer                            │                                  │
│  └───────────────────────────────────┘                                  │
│                                                                         │
│  Sign-in Template (side-by-side):                                       │
│  ┌──────────────────┬────────────────┐                                  │
│  │                  │                │                                  │
│  │  Sign-in Form    │  Side Image    │                                  │
│  │  (main slot)     │  (side slot)   │                                  │
│  │                  │                │                                  │
│  └──────────────────┴────────────────┘                                  │
│                                                                         │
│  Marketing Page Template:                                               │
│  ┌───────────────────────────────────┐                                  │
│  │ Header (Navbar)                   │                                  │
│  ├───────────────────────────────────┤                                  │
│  │ Main Content Sections             │                                  │
│  ├───────────────────────────────────┤                                  │
│  │ Footer                            │                                  │
│  └───────────────────────────────────┘                                  │
└────────────────────────────────┬────────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    RENDERED PAGE IN BROWSER                             │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🎬 Example: User Visits `/login`

```
Step 1: React Router
  URL: /login
  Matches: { path: '/login', pagePath: '/login' }

Step 2: Fetch Page JSON
  API: GET /api/v1/tenants/acme-corp/ui-config/
  Extract: pages["/login"]

Step 3: Receive Config
  {
    template: "sign-in",
    variant: "side-by-side",
    slots: {
      main: { component: "SignInForm", ... },
      side: { component: "Card", ... }
    }
  }

Step 4: Render Template
  Load: Sign-in Template (side-by-side variant)
  Fill: main slot with SignInForm
  Fill: side slot with Card

Step 5: Display
  User sees: Login form on left, image card on right
```

---

## 🗂️ Data Flow Diagram

```
┌──────────────┐
│   Database   │
│              │
│ • Tenant     │
│ • Routes     │
│ • UI Config  │
└──────┬───────┘
       │
       │ API Call
       ▼
┌──────────────┐
│   Django     │
│   Backend    │
│              │
│ Endpoints:   │
│ /tenants/... │
└──────┬───────┘
       │
       │ HTTP
       ▼
┌──────────────┐      ┌──────────────┐      ┌──────────────┐
│   Tenant     │─────▶│   Dynamic    │─────▶│   Json Page  │
│   Store      │      │   Routes     │      │   Route      │
└──────────────┘      └──────────────┘      └──────┬───────┘
                                                    │
                                                    ▼
┌──────────────┐      ┌──────────────┐      ┌──────────────┐
│  UI Library  │◀─────│    Page      │◀─────│  Use Json    │
│  Templates   │      │  Renderer    │      │    Pages     │
└──────────────┘      └──────────────┘      └──────────────┘
```

---

## 🔄 Configuration Hierarchy

```
Tenant Config (Database)
├── Branding
│   ├── Name
│   ├── Logo
│   └── Colors
├── Theme
│   ├── Preset ID
│   ├── Primary Color
│   └── Font Family
├── Routes                    ← Maps URLs to page paths
│   ├── / → /home
│   ├── /login → /login
│   └── /about → /about
└── Feature Flags

UI Config (Database)
└── Pages                     ← Maps page paths to templates
    ├── /home
    │   ├── template: landing-page
    │   └── slots: { header, main, footer }
    ├── /login
    │   ├── template: sign-in
    │   ├── variant: side-by-side
    │   └── slots: { main, side }
    └── /about
        ├── template: marketing-page
        └── slots: { header, main, footer }

Templates (UI Library)
├── landing-page
│   ├── Hero Section
│   ├── Features Section
│   └── Pricing Section
├── sign-in
│   ├── Centered Variant
│   └── Side-by-side Variant
└── marketing-page
    ├── Content Sections
    └── Sidebar Support
```

---

## 🎨 Template Slot Structure

### Landing Page Template
```
┌─────────────────────────────────┐
│  Slot: header                   │
│  (Navbar, AppBar, etc.)         │
├─────────────────────────────────┤
│  Slot: main                     │
│  ┌───────────────────────────┐  │
│  │ Hero Section              │  │
│  ├───────────────────────────┤  │
│  │ Features Section          │  │
│  ├───────────────────────────┤  │
│  │ Testimonials Section      │  │
│  ├───────────────────────────┤  │
│  │ Pricing Section           │  │
│  ├───────────────────────────┤  │
│  │ FAQ Section               │  │
│  └───────────────────────────┘  │
├─────────────────────────────────┤
│  Slot: footer                   │
│  (Footer with links, copyright) │
└─────────────────────────────────┘
```

### Sign-in Template (Side-by-side Variant)
```
┌───────────────────┬─────────────────────┐
│  Slot: main       │  Slot: side         │
│                   │                     │
│  ┌─────────────┐  │  ┌───────────────┐  │
│  │ Logo        │  │  │               │  │
│  ├─────────────┤  │  │               │  │
│  │ Title       │  │  │   Image or    │  │
│  ├─────────────┤  │  │  Testimonial  │  │
│  │ Email Input │  │  │    Content    │  │
│  ├─────────────┤  │  │               │  │
│  │ Password    │  │  │               │  │
│  ├─────────────┤  │  │               │  │
│  │ Submit Btn  │  │  └───────────────┘  │
│  ├─────────────┤  │                     │
│  │ Sign up     │  │                     │
│  │ link        │  │                     │
│  └─────────────┘  │                     │
└───────────────────┴─────────────────────┘
```

### Marketing Page Template
```
┌─────────────────────────────────┐
│  Slot: header                   │
│  (Navigation)                   │
├───────────────┬─────────────────┤
│  Slot: main   │  Slot: sidebar  │
│               │  (optional)     │
│  ┌─────────┐  │  ┌───────────┐  │
│  │Section 1│  │  │ Related   │  │
│  ├─────────┤  │  │ Content   │  │
│  │Section 2│  │  ├───────────┤  │
│  ├─────────┤  │  │ CTA Card  │  │
│  │Section 3│  │  └───────────┘  │
│  └─────────┘  │                 │
├───────────────┴─────────────────┤
│  Slot: footer                   │
└─────────────────────────────────┘
```

---

## 📦 Component Composition

### How Components Nest

```
PageRenderer
└── Template (landing-page)
    ├── Slot: header
    │   └── Component: Navbar
    │       └── props: { logo, links }
    │
    ├── Slot: main
    │   └── Component: LandingPage
    │       └── props: { sections }
    │           ├── Section: hero
    │           │   └── Component: HeroSection
    │           │       └── props: { title, subtitle, cta }
    │           │
    │           ├── Section: features
    │           │   └── Component: FeaturesSection
    │           │       └── props: { title, features }
    │           │
    │           └── Section: pricing
    │               └── Component: PricingSection
    │                   └── props: { title, plans }
    │
    └── Slot: footer
        └── Component: Footer
            └── props: { copyright, links }
```

---

## 🚀 Adding a New Page (Visual)

```
Step 1: Database (Add Route)
┌────────────────────────────────┐
│ routes: [                      │
│   ...,                         │
│   {                            │
│     path: "/pricing",      ←── Add this
│     pagePath: "/pricing", ←── And this
│     title: "Pricing",         │
│     protected: false,          │
│     layout: "none"             │
│   }                            │
│ ]                              │
└────────────────────────────────┘

Step 2: Database (Add Page JSON)
┌────────────────────────────────┐
│ pages: {                       │
│   ...,                         │
│   "/pricing": {            ←── Add this
│     template: "marketing-page",│
│     slots: {                   │
│       main: {                  │
│         component: "PricingPage"│
│         props: { ... }         │
│       }                        │
│     }                          │
│   }                            │
│ }                              │
└────────────────────────────────┘

Step 3: Result (Auto-generated Route)
┌────────────────────────────────┐
│ DynamicRoutes generates:       │
│                                │
│ <Route                         │
│   path="/pricing"              │
│   element={                    │
│     <JsonPageRoute             │
│       pagePath="/pricing"      │
│     />                         │
│   }                            │
│ />                             │
└────────────────────────────────┘

Step 4: Browser
┌────────────────────────────────┐
│ User visits:                   │
│ https://acme.com/pricing       │
│                                │
│ Sees: Marketing page template  │
│       with pricing content     │
└────────────────────────────────┘
```

---

## ✨ The Magic

**Zero code changes needed!**

Update JSON in database → Pages change instantly → Tenants happy 🎉
